import { DialogHeader, DialogTitle } from "@/components/ui/dialog";

export function JournalEntryModalHeader() {
  return (
    <DialogHeader>
      <DialogTitle>Create Journal Entry</DialogTitle>
    </DialogHeader>
  );
}